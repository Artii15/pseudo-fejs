CREATE KEYSPACE IF NOT EXISTS Test
WITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 4 };

USE Test;

DROP TABLE Users;

CREATE TABLE Users (
        nick varchar,
        PRIMARY KEY (nick)
        );
